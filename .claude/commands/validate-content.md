# Validate Content

Run the full content validation pipeline against all JSON in `content/`.

```bash
npx tsx scripts/validate-trove.ts
```

Report any schema violations, referential integrity errors, or substance score warnings.
