/**
 * Tactile PBR palette. Maps a human-readable tactile ID to the directory name
 * under public/assets/pbr/ that contains the material maps.
 *
 * Thornfield materials are added in task #6 (content curation).
 * Each value is the subdirectory name: public/assets/pbr/<directoryName>/
 */
export const PBR_PALETTE: Record<string, string> = {};
